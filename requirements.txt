requests
fastapi
uvicorn[standard]
# Resend (email) and Massive (market data) are called over REST via `requests` (no SDKs needed).

# PostgreSQL driver — used only when DATABASE_URL is set (required on Vercel;
# local dev and the test suite fall back to SQLite and never import this).
psycopg[binary]

# Local AI is NOT here on purpose: llama-cpp-python compiles llama.cpp and the
# Qwen GGUF is ~1GB, which cannot fit or build in a serverless function. Install
# it for local/Docker runs with:  pip install -r requirements-local.txt
